apiVersion: v1
kind: Service
metadata:
  name: ironflow-gateway
  labels:
    app: ironflow-gateway
spec:
  selector:
    app: ironflow-gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
